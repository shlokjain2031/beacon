import 'package:flutter/cupertino.dart';

class UniversitiesPromptSplashScreen extends StatelessWidget {
  const UniversitiesPromptSplashScreen({super.key});

  // todo: stream build the edge function and show a splash screen till data visible. Move to dashboard home once finish

  @override
  Widget build(BuildContext context) {
    return StreamBuilder(
      stream: ,
      builder: () {},
    );
  }

}